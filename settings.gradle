pluginManagement {
  repositories {
    mavenLocal()
    maven { url "https://dl.google.com/spinnaker/gradle/" }
    gradlePluginPortal()
  }
}

rootProject.name = "gcs-account-source-plugin"

include "gcs-account-registration-plugin"

def setBuildFile(project) {
  project.buildFileName = "${project.name}.gradle"
  project.children.each {
    setBuildFile(it)
  }
}

rootProject.children.each {
  setBuildFile(it)
}

